---
sudo: false

language: python

python:
  - "3.4"
  - "3.5"
  - "3.6"
  - "3.7"

install:
  - pip install --upgrade .[dev]
  - pip install codecov

before_script:
  - pip list

script:
  - pytest

after_success:
  - codecov

before_cache:
  - rm -rf $HOME/.cache/pip/log

cache: pip
